# Day 14：端到端联调验收

## 当前结论

Day 14 已完成。

这一阶段没有继续新增主功能，而是：

**把 Week 2 现有的问答主链路做了一次完整验收，并把真实结果固定了下来。**

经过 Day 13，项目已经具备：

1. 知识库创建
2. 文档上传
3. 文档解析
4. 文本切块
5. chunk 入库
6. chunk embedding
7. pgvector 检索
8. Prompt 组装
9. OpenAI-compatible chat completion
10. `/qa/ask`
11. `sources` 结构化来源
12. 问答记录持久化
13. `/qa/history`

所以 Day 14 的核心不是“继续堆功能”，而是：

**验证主链路稳定性、可解释性和演示完整度。**

## Day 14 实际完成了什么

今天实际完成了下面几件事：

1. 做一次从上传到问答历史的完整联调
2. 固定一组可复现的验收样例
3. 检查返回结构是否稳定
4. 检查失败路径和空结果路径
5. 更新 README 和进度文档

## Day 14 验收范围

本次按下面顺序做了验收：

1. 创建知识库
2. 上传文档
3. 触发解析与分块
4. 触发 embedding
5. 调用 `/qa/retrieve`
6. 调用 `/qa/ask`
7. 调用 `/qa/history`

每一步至少确认：

1. 接口返回成功
2. 关键字段完整
3. 数据状态正确
4. 能衔接到下一步

## Day 14 重点检查项

本次重点核对了这几类问题：

1. 检索结果是否稳定
2. 回答是否严格基于召回内容
3. `sources` 和 `retrievalResults` 是否一致
4. 问答记录是否写库成功
5. 历史查询是否能查回最新记录
6. 空召回时的回答是否符合预期

## Day 14 真实验收结果

本次使用新的 `day14-kb` 做了完整联调：

1. 已创建知识库 `day14-kb`
2. 已上传文档 `Week2 Acceptance Plan`
3. 已触发 `/process`，生成 `16` 个 chunk
4. 已触发 `/embed`，完成 `16` 个 chunk 向量写库
5. `GET /qa/readiness` 已返回 `questionAnsweringReady = true`
6. `POST /qa/retrieve` 已返回 Week 2 目标相关 TopK 结果
7. `POST /qa/ask` 对问题“第2周的目标是什么？”返回了正确答案：`让项目从“能跑”变成“像样”`
8. `POST /qa/ask` 对无关问题“法国的首都是什么？”返回了：`根据当前检索内容，无法确定答案。`
9. `GET /qa/history` 已查回两条真实问答记录

这说明当前系统已经通过下面两类关键验收：

1. 正常问题场景可以完成“检索 -> 回答 -> 来源返回 -> 持久化 -> 历史查询”
2. 无关问题场景会遵守“只基于检索内容回答”的约束

## Day 14 输出物

今天结束后，已经产出：

1. 一组完整 curl 样例
2. 一次真实联调结果说明
3. README 中的验收章节
4. 当前项目状态更新

## Day 14 完成后的项目状态

Day 14 完成后，Week 2 的最小 RAG 闭环已经完成第一版收口：

```text
知识库创建 -> 文档上传 -> 解析 -> 切块 -> chunk 入库 -> chunk 向量写库 -> query embedding -> TopK 检索 -> Prompt 组装 -> LLM 回答 -> 来源返回 -> 问答记录持久化 -> 历史查询 -> 端到端验收
```

如果后续继续推进，可以考虑：

1. 来源引用格式化
2. session 复用
3. 多轮对话
4. 问答效果优化
