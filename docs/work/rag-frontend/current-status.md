# 当前状态

## 当前结论

前端已经不是纯规划状态，而是已经完成了第一版工作台实现，并和后端现有主链路做过真实联调。

当前前端真实能力可以概括为：

1. 已具备知识库、文档、检索、问答、历史、健康页的第一版页面骨架。
2. 已接入后端统一 `ApiResponse` 契约和 requestId 错误展示。
3. 已完成 `DENSE / HYBRID` 检索模式、`retrievalMode / fusionStrategy` 展示与历史回放适配。
4. 已适配 rerank 状态、模型、候选数、耗时，以及召回分/重排分双分数展示；启用与否仍由后端配置控制。
5. 已具备知识库恢复、文档禁用/恢复、重新嵌入等运维入口的第一版交互承接。

当前前端的真实定位不是“展示 demo 页”，而是：

**围绕知识库工作台、文档接入、检索调试、问答和历史回放的一版企业后台式 RAG 操作台。**

## 已完成

### 页面与路由

1. 知识库列表页已落地。
2. Dashboard / 工作台页已落地。
3. 文档上传页已落地。
4. 文档列表页已落地。
5. 文档详情页已落地。
6. 检索调试页已落地。
7. 问答页已落地。
8. 问答历史页已落地。
9. 健康页已落地。

### 组件与交互

1. `AppShell` 已统一全局布局。
2. `StatusBadge` 已统一 KB / 文档 / 任务状态展示。
3. `ReadinessCard` 已承接 `qa/readiness` 的核心提示。
4. `IndexingTimeline` 已承接索引任务阶段展示。
5. `ChunkInspector` 已承接切块与证据检视。
6. `RetrievalResultList` 已承接检索命中展示。
7. `AnswerCard` 已承接回答结果展示。
8. `SourceList` 已承接来源回溯展示。
9. `RetryActionBar` 已承接失败重试操作。
10. `ApiErrorAlert` 已统一展示 message 与 requestId。

### API 接入

1. 知识库列表、创建、启用、禁用、删除已接入。
2. 文档上传、列表、详情、chunks、索引任务查询已接入。
3. 文档禁用 / 恢复与失败任务重试已接入。
4. `qa/readiness`、`qa/retrieve`、`qa/ask`、`qa/history` 已接入。
5. `health` 与 `redis-probe` 已接入。
6. `POST /api/admin/embeddings/rebuild` 已接入运维入口。
7. 健康页当前已适配 `aiGateway / embedding / llm` 的实时 provider / model 展示，不再只显示旧的本地静态默认值。

### 契约适配

1. 前端已适配后端统一 `ApiResponse<T>` 响应结构。
2. 前端已适配 `retrievalMode / fusionStrategy` 字段。
3. 前端已适配 `qa/history` 的新旧检索快照兼容。
4. 前端已适配知识库与文档恢复相关状态语义。
5. 前端已适配 `rerankStatus / rerankModel / rerankDurationMs / rerankScore` 契约。

## 已验证

当前已经完成过的真实验证包括：

1. `npm run build` 已通过。
2. 前端生产构建已完成路由级懒加载与 vendor 拆包。
3. 已完成一次真实前后端联调，覆盖：
4. 知识库切换。
5. 文档接入与索引状态观察。
6. `DENSE / HYBRID retrieve`。
7. `HYBRID ask`。
8. `qa/history` 回放。
9. 健康页展示。
10. 2026-05-20 已验证健康页在 Gateway chat 默认模型切换到 `qwen-plus` 后，会同步显示新的 provider / model。

## 当前未完成

1. `plan.md` 仍然比实际实现更详细，部分计划项和真实完成项还没有完全分离。
2. 还没有继续细化业务级按需加载和更深层性能优化。
3. 还没有补更完整的前端交互验收清单和页面级测试。
4. 前端板块还缺少更细的阶段历史记录，目前主要依赖状态和计划文档。

## 当前判断

当前前端阶段更准确的表达是：

1. 第一版工作台式前端已经完成。
2. 它已经不只是静态壳子，而是和当前后端主链路做过真实联调。
3. 它的主要价值在于把“RAG 主链路 + 运维入口 + 证据回放”组织成可演示的产品形态。
4. 后续前端工作重点不应再只是补页面数量，而应转向状态收口、联调稳定性和体验打磨。
5. 系统健康页已经具备第一版“异常优先 + 高延迟提示 + AI Gateway 分组展示”的运维视图，而不是单纯把 `/api/health` 原样平铺成一张表。

## 后续方向

建议前端板块下一步优先做这些事：

1. 把 `plan.md` 中已完成和未完成项进一步拆开。
2. 增加页面级状态快照和联调记录，减少“只有计划、没有状态”的文档结构失衡。
3. 跟随后端 AI Gateway、retrieval 评测和会话能力演进，逐步补对应页面或运维入口。
4. 如果后续推进 session reuse / 多轮问答，优先补历史页和问答页的会话线程表达，而不是先扩更多独立页面。
