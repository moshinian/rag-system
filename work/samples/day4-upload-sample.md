# Settlement Upload Sample

## Overview

This markdown file is used to verify the upload path before parsing and chunking are implemented.

## Key Points

1. Upload should persist metadata to PostgreSQL.
2. The original file should be stored under `data/uploads`.
3. The initial document status should remain `UPLOADED`.
