apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rag-system
  namespace: rag-system
spec:
  rules:
    - host: rag.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rag-frontend
                port:
                  number: 80
